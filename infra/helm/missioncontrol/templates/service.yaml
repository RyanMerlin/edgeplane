apiVersion: v1
kind: Service
metadata:
  name: {{ include "edgeplane.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "edgeplane.fullname" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8008
