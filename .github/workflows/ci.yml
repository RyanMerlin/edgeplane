name: CI
on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:
    inputs:
      run_compat_smoke:
        description: "Run compatibility smoke matrix"
        required: false
        type: boolean
        default: false

jobs:
  rust-test:
    name: Rust tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - name: Install cargo-nextest
        uses: taiki-e/install-action@v2
        with:
          tool: nextest
      - name: Cache Cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-test-${{ hashFiles('crates/mc/Cargo.lock', 'crates/mc-controlplane/Cargo.lock', 'crates/mcd/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-test-
      - name: Test mc-controlplane
        run: cargo nextest run --manifest-path crates/mc-controlplane/Cargo.toml --test test_health --test test_routes --test test_proxy --test test_work
      - name: Test mc
        run: cargo nextest run --manifest-path crates/mc/Cargo.toml --test-threads 1
      - name: Test mcd
        run: cargo nextest run --manifest-path crates/mcd/Cargo.toml --workspace

  launch-regression:
    name: Launch regression
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - name: Cache Cargo registry
        uses: actions/cache@v5
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
          key: ${{ runner.os }}-cargo-launch-regression-${{ hashFiles('crates/mc/Cargo.lock') }}
          restore-keys: ${{ runner.os }}-cargo-launch-regression-
      - name: Run mc launch regression harness
        run: bash scripts/launch-regression.sh

  compat-smoke:
    if: github.event_name == 'workflow_dispatch' && inputs.run_compat_smoke == true
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable
      - name: Run staged compatibility smoke matrix
        run: |
          cd crates/mc
          cargo run -- system compat matrix run --providers claude,codex --mode smoke

  compat-strict-main:
    if: github.ref == 'refs/heads/main' && github.event_name == 'push'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - name: Check whether mc integration changed
        id: changes
        uses: dorny/paths-filter@v4
        with:
          filters: |
            mc:
              - 'crates/mc/**'
      - uses: dtolnay/rust-toolchain@stable
        if: steps.changes.outputs.mc == 'true'
      - name: Main branch strict compatibility gate
        if: steps.changes.outputs.mc == 'true'
        run: |
          cd crates/mc
          mkdir -p .ci-bin
          cat > .ci-bin/claude <<'EOF'
          #!/usr/bin/env bash
          exit 0
          EOF
          cat > .ci-bin/codex <<'EOF'
          #!/usr/bin/env bash
          exit 0
          EOF
          chmod +x .ci-bin/claude .ci-bin/codex
          export PATH="$PWD/.ci-bin:$PATH"
          cargo run -- system compat matrix run --providers claude,codex --mode smoke > /tmp/compat-latest.json
          jq -e '.summary.overall_status == "compatible"' /tmp/compat-latest.json >/dev/null
      - name: Compatibility gate skipped
        if: steps.changes.outputs.mc != 'true'
        run: echo "No crates/mc changes detected; skipping strict compatibility gate."

  docs-catalog:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.12"
      - run: pip install pyyaml
      - run: python scripts/validate-doc-catalog.py
